#include<stdio.h>


void peak(int queue[], int frntInfo[]) {
    int i = 0;
    while (0==0) {
        if (queue[i] == NULL) {
            frntInfo[0] = queue[i-1];
            frntInfo[1] = i-1;
            return;
        }
        i++;
    }
}